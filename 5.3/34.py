import numpy as np

cos_value = np.cos(3)
print(f"cos value: {cos_value}")
exp_value = np.exp(3)
print(f"exp value: {exp_value}")

print(f"cos value * 2: {cos_value * -1}")

print(f"cos value * 2: {cos_value * -1}")

result = (-2 * cos_value) - exp_value + 3

print(result)